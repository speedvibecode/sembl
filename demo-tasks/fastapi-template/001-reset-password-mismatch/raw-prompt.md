in the dashboard, resetting a password is broken — i open the reset password page,
type the same password in both fields and it always errors with "The passwords do not
match" even though they are literally identical. so nobody can ever complete a
password reset. signup/login still work fine. please fix the reset password flow.
