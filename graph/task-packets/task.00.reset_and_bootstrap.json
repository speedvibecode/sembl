{
  "task_id": "task.00.reset_and_bootstrap",
  "agent": "Mechanic",
  "scope": "Repo reset and graph artifact bootstrap.",
  "allowed_context": ["repo_root_inventory", "sembl_docs_inventory", "cleanup_policy"],
  "forbidden_context": ["unrestricted_repo_context", "provider_secrets"],
  "inputs": ["hard reset plan", "repo root path"],
  "outputs": ["clean root inventory", "regenerated artifact directories"],
  "acceptance": ["Only `.git`, `sembl_docs`, `graph`, `scripts`, `package.json`, and `.gitignore` are present after first-run artifact regeneration."]
}
