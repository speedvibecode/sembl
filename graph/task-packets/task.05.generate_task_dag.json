{
  "task_id": "task.05.generate_task_dag",
  "agent": "Planner",
  "scope": "Generate executable task graph and scoped packets.",
  "allowed_context": ["normalized graph", "agent roster policy", "task graph invariants"],
  "forbidden_context": ["unbounded worker context", "implicit execution ordering"],
  "inputs": ["graph/normalized_graph.json", "graph/agents/agent_roster.json"],
  "outputs": ["graph/task_graph.json", "graph/task-packets/*.json"],
  "acceptance": ["Task DAG is acyclic.", "Every task has a named agent and model policy.", "Build tasks map to execution boundaries.", "Worker packets are scoped."]
}
